{
  "name": "tsg-yield-engine",
  "version": "0.1.0",
  "private": true,
  "description": "TSG Yield Engine - rekenlaag (Fase 1). Pure TypeScript, replicates the corrected TSG_Model_v2.xlsx.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
